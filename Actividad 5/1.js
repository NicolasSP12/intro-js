var arreglo = [1, 4, 6, 10, 22, 55, 46, 2, 5, 0]

for(var i = 0; i < arreglo.length; i++) { 
    // console.log(arreglo[i]) Para que se iteren todos los numeros
    if(arreglo[i]>3){console.log(arreglo[i])} //Para que solo se iteren los numeros mayores a 3
}